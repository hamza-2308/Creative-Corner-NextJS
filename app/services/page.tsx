import { db } from "@/lib/db"; import Link from "next/link";
export const dynamic = "force-dynamic";
export default async function Services(){const services=await db.service.findMany({where:{active:true},orderBy:{name:"asc"}});return <><section className="page-hero"><div className="container"><div className="eyebrow">Our Services</div><h1>Everything your event needs.</h1><p>From planning and décor to production, catering and photography.</p></div></section><section className="section"><div className="container">{services.length?<div className="grid grid-3">{services.map(s=><div className="card" key={s.id}><div className="image-box"><img src={s.image} alt={s.name}/></div><div className="card-body"><h3>{s.name}</h3><p className="muted">{s.description}</p><ul className="feature-list">{s.features.split("|").map(f=><li key={f}>{f}</li>)}</ul>{s.price&&<p className="price">Starting PKR {s.price.toLocaleString()}</p>}<Link className="btn btn-primary" href={`/booking?service=${s.id}`}>Book Now</Link></div></div>)}</div>:<div className="empty">No services found. Run <code>npm run db:setup</code> to load sample data.</div>}</div></section></>}
